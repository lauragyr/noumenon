function size(parameter) {
  if (a1 == 2) {
    radius = 10;
    rotate(-generalAngle);
    polygon(pos, pos, radius, npoints);
    generalAngle = generalAngle + distance;

  } else if (a1 == 3) {
    radius = 30;
    rotate(-generalAngle);
    polygon(pos, pos, radius, npoints);
    generalAngle = generalAngle + distance;

  } else if (a1 == 4) {
    radius = 60;
    rotate(-generalAngle);
    polygon(pos, pos, radius, npoints);
    generalAngle = generalAngle + distance;

  } else if (a1 == 5) {
    radius = 120;
    rotate(-generalAngle);
    polygon(pos, pos, radius, npoints);
    generalAngle = generalAngle + distance;
  }
}
